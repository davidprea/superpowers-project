DATABASE_URL=postgresql://localhost:5432/superpowers
JWT_SECRET=your-secret-key-change-this
RESEND_API_KEY=
FROM_EMAIL=noreply@superpowersproject.org
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=Admin
NODE_ENV=development
PORT=3001
# Comma-separated list of allowed browser origins for CORS.
# Dev: leave the Vite origin so the proxy works. Prod: your deployed frontend URL.
CORS_ORIGIN=http://localhost:5173
# Public frontend URL — used to build unsubscribe links in outgoing emails.
SITE_URL=http://localhost:5173

# Cloudflare R2 (image uploads)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PUBLIC_URL=
